using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LvlImage : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       // GameObject level = Instantiate(Resources.Load<GameObject>("Prefabs/Circle"), new Vector3(0,0,0), Quaternion.identity);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
